<!-- ACE WATCH -->
<section class="ace-watch" id="acewatch">
  <div class="ace-content">
    <span class="section-tag">// 006</span>
    <h2>THE ACE WATCH</h2>
    <p class="ace-subtitle">CHILL GOOD NIGHT'S FIRST ACE</p>
    <div class="ace-counter">
      <div class="ace-stat">
        <span class="ace-num count-up" id="days-count" data-target="0">0</span>
        <span class="ace-label">DAYS SINCE<br/>SQUAD FORMED</span>
      </div>
      <div class="ace-divider"></div>
      <div class="ace-stat">
        <span class="ace-num count-up" data-target="0">0</span>
        <span class="ace-label">ACES<br/>ACHIEVED</span>
      </div>
      <div class="ace-divider"></div>
      <div class="ace-stat">
        <span class="ace-num">&infin;</span>
        <span class="ace-label">HOPE<br/>REMAINING</span>
      </div>
    </div>
    <p class="ace-quote">"Look me in the eyes and tell me you believe that."</p>
    <p class="ace-attribution">— CHILL GOOD NIGHT, when told tonight could be the night</p>
  </div>
</section>

<script>
  const start = new Date('2020-07-01');
  const now = new Date();
  const days = Math.floor((now.getTime() - start.getTime()) / (1000 * 60 * 60 * 24));
  const el = document.getElementById('days-count');
  if (el) {
    el.dataset.target = days.toString();
  }
</script>

<style>
  .ace-watch {
    padding: 6rem 3rem;
    text-align: center;
    position: relative;
    overflow: hidden;
    border-top: 1px solid rgba(245,214,35,0.1);
    border-bottom: 1px solid rgba(245,214,35,0.1);
    background: radial-gradient(ellipse at center, rgba(245,214,35,0.03) 0%, transparent 60%);
  }

  .ace-content {
    position: relative;
    z-index: 2;
  }

  .section-tag {
    font-size: 0.7rem;
    letter-spacing: 4px;
    color: var(--val-red);
    font-weight: 600;
    text-shadow: 0 0 10px rgba(255,70,85,0.3);
  }

  .ace-watch h2 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 700;
    letter-spacing: 6px;
    margin-top: 0.5rem;
  }

  .ace-subtitle {
    font-size: 0.8rem;
    letter-spacing: 5px;
    opacity: 0.4;
    margin-top: 0.5rem;
    font-weight: 600;
  }

  .ace-counter {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 3rem;
    margin: 3rem 0;
    padding: 2rem;
    background: rgba(245,214,35,0.03);
    border: 1px solid rgba(245,214,35,0.1);
    display: inline-flex;
    clip-path: polygon(16px 0, calc(100% - 16px) 0, 100% 16px, 100% calc(100% - 16px), calc(100% - 16px) 100%, 16px 100%, 0 calc(100% - 16px), 0 16px);
  }

  .ace-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .ace-num {
    font-size: 3.5rem;
    font-weight: 700;
    color: #f5d623;
    text-shadow: 0 0 30px rgba(245,214,35,0.3);
    line-height: 1;
  }

  .ace-label {
    font-size: 0.55rem;
    letter-spacing: 3px;
    opacity: 0.4;
    margin-top: 0.75rem;
    font-weight: 600;
    line-height: 1.4;
  }

  .ace-divider {
    width: 1px;
    height: 50px;
    background: linear-gradient(180deg, transparent, rgba(245,214,35,0.3), transparent);
  }

  .ace-quote {
    font-size: 1.3rem;
    font-style: italic;
    opacity: 0.5;
    margin-top: 2rem;
  }

  .ace-attribution {
    font-size: 0.7rem;
    letter-spacing: 3px;
    opacity: 0.25;
    margin-top: 0.5rem;
    font-weight: 600;
  }
</style>
